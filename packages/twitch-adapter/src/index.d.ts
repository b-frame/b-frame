export class TwitchAdapter {
	constructor(
		config: {
			capabilities: string[],
			channels: string[],
			credentials: {
				username: string,
				token: string,
			},
			prefix: string | string[],
			websocketURL: string,
		}
	)
}
