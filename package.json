{
	"name": "root",
	"private": true,
	"scripts": {
		"build": "yarn build:cli && yarn build:base-adapter && yarn build:discord-adapter && yarn build:twitch-adapter",
		"build:cli": "yarn workspace @b-frame/cli build",
		"build:base-adapter": "yarn workspace @b-frame/base-adapter build",
		"build:discord-adapter": "yarn workspace @b-frame/discord-adapter build",
		"build:twitch-adapter": "yarn workspace @b-frame/twitch-adapter build",
		"cz": "cz",
		"dev": "yarn workspace test-app dev",
		"test": "yarn test:cli && yarn test:base-adapter && yarn test:discord-adapter && yarn test:twitch-adapter",
		"test:base-adapter": "yarn workspace @b-frame/base-adapter test",
		"test:cli": "yarn workspace @b-frame/cli test",
		"test:discord-adapter": "yarn workspace @b-frame/discord-adapter test",
		"test:twitch-adapter": "yarn workspace @b-frame/twitch-adapter test"
	},
	"workspaces": [
		"packages/cli",
		"packages/base-adapter",
		"packages/discord-adapter",
		"packages/twitch-adapter",
		"packages/test-app"
	],
	"devDependencies": {
		"@commitlint/cli": "^11.0.0",
		"@commitlint/config-conventional": "^11.0.0",
		"commitizen": "^4.2.1",
		"husky": "^4.3.0"
	}
}
